# FatigueSense pipeline — Python dependencies
#
# Use with a conda env (see README.md):
#   conda create -n fatigue-sense python=3.11 -y
#   conda activate fatigue-sense
#   conda install pytorch torchvision pytorch-cuda=12.4 -c pytorch -c nvidia -y
#   pip install -r requirements.txt

# --- Core ML (inference + training) ---
torch==2.6.0
torchvision==0.21.0
numpy>=1.26,<3

# --- Vision / face ---
mediapipe>=0.10.9
opencv-python>=4.8,<5
ultralytics>=8.3,<9

# --- Data / HF ---
pandas>=2.1,<3
pyarrow>=14
huggingface_hub>=0.23

# --- Training utilities ---
scikit-learn>=1.4,<2
tqdm>=4.66
PyYAML>=6
Pillow>=10
safetensors>=0.4
